"use client";
import { useRef, useState } from "react";
import { useAssistant } from "@/components/assistant-provider";
const prompts = ["Recruiter: is he a fit for a GenAI / RAG role?", "Hiring manager: walk me through the KPMG Context Layer", "Tech lead: multi-agent / LangGraph experience?", "Peer: Databricks + governance depth?", "How do I reach him?"];
export function AssistantHeroQuery() { const [value, setValue] = useState(""); const inputRef = useRef<HTMLInputElement>(null); const { ask, loading } = useAssistant(); return <div><form onSubmit={(event) => { event.preventDefault(); void ask(value, inputRef.current); }}><label className="sr-only" htmlFor="hero-query">Ask about Dhruv Dave&apos;s fit, work, or contact details</label><div className="flex min-h-14 items-center gap-3 border border-line bg-surface px-4 focus-within:border-accent"><span className="font-mono text-meta text-ink-3">Query ›</span><input aria-busy={loading} className="min-w-0 flex-1 bg-transparent font-mono text-meta text-ink outline-none placeholder:text-ink-3" id="hero-query" onChange={(event) => setValue(event.target.value)} placeholder="Ask about my fit, my work, or how to reach me…" ref={inputRef} value={value} /><button className="font-mono text-meta text-accent" disabled={loading} type="submit">Send</button></div></form><div aria-label="Suggested assistant questions" className="mt-4 flex flex-wrap gap-2">{prompts.map((prompt) => <button className="record-tag border border-line px-2.5 py-1 text-left font-mono text-meta leading-5 text-ink-2" key={prompt} onClick={(event) => void ask(prompt, event.currentTarget)} type="button">{prompt}</button>)}</div></div>; }
